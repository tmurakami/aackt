apply from: "$rootDir/gradle/android-library.gradle"

dependencies {
    api deps.androidx.room.rxjava2
    testImplementation deps.androidx.room.testing
}

dokka {
    externalDocumentationLink {
        url = 'http://reactivex.io/RxJava/2.x/javadoc/'.toURL()
    }
}
