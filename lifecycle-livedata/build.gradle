apply from: "$rootDir/gradle/android-library.gradle"

dependencies {
    api deps.androidx.lifecycle.livedata
    testImplementation deps.androidx.arch.core.testing
    testImplementation deps.androidx.lifecycle.runtime
}

dokka.samples = android.sourceSets.test.java.srcDirs.collect { it.path }
