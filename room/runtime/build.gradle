apply from: "$rootDir/gradle/android-library.gradle"

group = 'com.github.tmurakami.aackt.persistence.room'

dependencies {
    api deps.arch.persistence.room.runtime
    testImplementation deps.arch.persistence.room.testing
}
